#include <stdio.h>

int main()
{
  int angFreq=314;
  char str[500]= "10||(200F + 2H + (6||500F))";
  char operator[50]; int oI=0;
  char ar[50][3]; int
  for(int i=0; str[i]!='\0'; ++i])
  {
    int j=i;
    for(j; str[j]!='\0' && isdigit(str[j]); ++j)
    {

    }
  }
}
