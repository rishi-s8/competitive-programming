#include <iostream>
using namespace std;

int main()
{
  int n, temp;
  cin >> n;
  vector<int> A,B;
  for(int i=0; i<n; ++i)
  {
    cin >> temp;
    A.push_back(temp);
  }
  for(int i=0; i<n; ++i)
  {
    cin >> temp;
    B.push_back(temp);
  }
  long long sum = 0;
  for(int i=0; i<n; ++i)
  {
    for(int j=i; j<n; ++j)
    {
      
    }
  }
}
