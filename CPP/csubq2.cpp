#include <bits/stdc++.h>
using namespace std;
int main()
{
  int ar[100001]={0};
  int n,q,L,R,t,x,y,count;
  scanf("%d%d%d%d", &n,&q,&L,&R);
  while(q--)
  {
    count=0;
    scanf("%d%d%d", &t,&x,&y);
    if(t==1)
      ar[x]=y;
    else
    {
      
    }
  }
}
